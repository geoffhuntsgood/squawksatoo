import { colors } from "@mui/material";

export const dkColors = {
  textColor: "white",
  outlineColor: colors.grey[700],
  focusColor: colors.blue.A700,
  backgroundColor: colors.grey[900],
  disabledColor: colors.blueGrey[500],
  buttonColor: colors.blue[900]
};
